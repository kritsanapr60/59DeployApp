import { AdminTemplateDirective } from './admin-template.directive';

describe('AdminTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminTemplateDirective();
    expect(directive).toBeTruthy();
  });
});

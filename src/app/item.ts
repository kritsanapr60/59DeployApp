/**
 * Interface for classes that item Item data
 *
 * @export
 * @interface Item
 */
export interface Item {
  /**
   * เก็บค่าตัวเลขที่ได้จาก firebase
   * @type {number}
   * @memberof Item
   */
  value: number;
}
